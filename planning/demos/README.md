# Demos (presentations & visual tests)

| Demo | Path |
|------|------|
| **Playwright draft solution** (mock + live API screenshots) | `draft-solution-demo/README.md` |

Regenerate PNGs from that folder: `npm run test:mock` or `npm test`.
