# Stakeholder presentation pack (filled mock)

**Purpose:** One place for a **client-ready narrative**: setting, **3 use cases**, sample dialogue, and **mock API JSON** aligned with `solution/sample_data/financial_sample.csv` (7 rows).

| Document | Use |
|----------|-----|
| **`full-demo-scenario.md`** | Full story + use cases 1–3 + slide bullets + bilingual examples |
| **`mock-api/*.json`** | Exact JSON shapes you can paste into slides or Swagger screenshots |
| **`speaker-outline.md`** | Order of demo / timing |

**Master plan:** referenced from `planning/solution-build-master-steps.md` (step **15a**).
