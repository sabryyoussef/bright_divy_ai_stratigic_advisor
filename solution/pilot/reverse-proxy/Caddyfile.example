# Example Caddyfile for pilot (Step 17).
# Copy to Caddyfile, replace domains and upstream addresses.

# Optional: one host for Dify
# dify.pilot.example.com {
#     reverse_proxy 127.0.0.1:80
# }

# API host for FastAPI (host port 18001 = docker compose from stack/compose.stack.yml)
# api.pilot.example.com {
#     reverse_proxy 127.0.0.1:18001
# }

# ---- Self-signed / lab only (not for production) ----
# localhost {
#     tls internal
#     reverse_proxy 127.0.0.1:18001
# }
